import { admin, body, cors, fail, ok, user, vector } from '../_shared/http.ts'; import { contentHash, generateEmbedding } from '../_shared/gemini.ts';
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});const actor=await user(req),i=await body(req);if(!actor)return fail('unauthorized','Authentication is required',401);if(!i||typeof i.learner_id!=='string')return fail('validation_error','learner_id is required');if(i.learner_id!==actor.id)return fail('forbidden','Cannot embed another learner',403);const db=admin(),{data,error}=await db.from('learner_profiles').select('*').eq('user_id',actor.id).single();if(error)return fail('not_found','Learner profile not found',404);try{const text=JSON.stringify({education_level:data.education_level,subjects:data.subjects,interests:data.interests,location:data.location,learning_goals:data.learning_goals,budget_min:data.budget_min,budget_max:data.budget_max,availability:data.availability});const hash=await contentHash(text);if(data.profile_embedding&&data.embedding_input_hash===hash)return ok({embedded:true,cached:true});const e=await generateEmbedding(text);const {error:u}=await db.from('learner_profiles').update({profile_embedding:vector(e),embedding_input_hash:hash,updated_at:new Date().toISOString()}).eq('user_id',actor.id);if(u)throw u;return ok({embedded:true,cached:false});}catch(e){return fail('embedding_failed',e instanceof Error?e.message:'Embedding failed',502);}});

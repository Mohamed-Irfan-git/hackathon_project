import { admin, body, cors, fail, ok, user } from '../_shared/http.ts';
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});const actor=await user(req),i=await body(req);if(!actor)return fail('unauthorized','Authentication is required',401);if(!i||!Number.isFinite(i.amount)||Number(i.amount)<=0||('learner_id'in i&&i.learner_id!==null&&typeof i.learner_id!=='string')||('opportunity_id'in i&&i.opportunity_id!==null&&typeof i.opportunity_id!=='string'))return fail('validation_error','amount must be positive; IDs must be UUID strings or null');const {data,error}=await admin().from('sponsorships').insert({sponsor_id:actor.id,learner_id:i.learner_id??null,opportunity_id:i.opportunity_id??null,amount:i.amount}).select('id,status').single();return error?fail('internal_error',error.message,500):ok({sponsorship_id:data.id,status:data.status});});

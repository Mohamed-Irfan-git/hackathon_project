import { admin, body, cors, fail, ok, user } from '../_shared/http.ts';
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});const actor=await user(req),i=await body(req);if(!actor)return fail('unauthorized','Authentication is required',401);if(!i||typeof i.opportunity_id!=='string')return fail('validation_error','opportunity_id is required');const db=admin(),{data:opp}=await db.from('opportunities').select('id,status').eq('id',i.opportunity_id).single();if(!opp||opp.status!=='active')return fail('not_found','Active opportunity not found',404);const {data,error}=await db.from('bookings').insert({learner_id:actor.id,opportunity_id:i.opportunity_id}).select('id,status').single();return error?fail('internal_error',error.message,500):ok({booking_id:data.id,status:data.status});});
